'use client';

import { Container, Text } from '@/components/slices';

export const ListingsCRUD: React.FC = () => {
  return (
    <Container>
      <Text variant="h2">Listings Management</Text>
      <Text variant="paragraph">
        Listings management is under development.
      </Text>
    </Container>
  );
};
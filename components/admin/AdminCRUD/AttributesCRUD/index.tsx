'use client';

import { Container, Text } from '@/components/slices';

export const AttributesCRUD: React.FC = () => {
  return (
    <Container>
      <Text variant="h2">Attributes Management</Text>
      <Text variant="paragraph">
        Attributes management is under development.
      </Text>
    </Container>
  );
};